export const routes = {
    main: '/:groupId/feed',
    news: '/:groupId/news',
    members: '/:groupId/members',
    schedule: '/:groupId/schedule',
    user: '/user/:id',
    auth: {
        signin: '/signin'
    }
}